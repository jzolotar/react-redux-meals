import { StyledHeader } from './styles/Header.styled';

const Header = (props) => {
  return (
    <StyledHeader>
      <h1>Our Menu</h1>
      <div></div>
    </StyledHeader>
  );
};

export default Header;
