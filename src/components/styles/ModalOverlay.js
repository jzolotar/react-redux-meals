import styled from 'styled-components';

export const ModalOverlay = styled.section`
  z-index: 1000;
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: #ffff;
  opacity: 0.75;
`;
